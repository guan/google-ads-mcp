/**
 * MCP Server Setup
 *
 * Initializes the Model Context Protocol server with tool registration.
 * This module is imported by the main entry point.
 */

export { tools } from '../tools/index.js';
